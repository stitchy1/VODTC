启动：
nohup python3 count.py >/dev/null 2>&1 &

画线：
须设置参数 --show_video 1，方向由其他参数设置
画面中按下 “i” 或 “o” 画 in线 或 out线，画好线开始计数，可反复重新画线
画面中按下 “r” 重置所有画线
画面中按下 “Esc” 退出实时视频，开启日志记录
命令行键入 “v” 并回车，打开实时视频
命令行键入 “q” 并回车，结束程序
画面中按下 “q” 结束程序

特别注意：手动画线时，过线判断由计数点在直线方程上的函数值正负决定，这并非直觉上的感受，一些情况下，计数方向设置了“d”，但物体从下向上运动时也可能会被计数，或从上向下运动未被计数，这是画线斜率和设置方向不匹配而违背了直觉造成的，当过于不匹配时，画面将会提醒“invalid line with direction”。因此建议，当方向设置为“u”或“d”时，线尽量画的水平一些，方向设置为“r”或“l”时，线尽量竖直一些。

参数：
由上到下按照使用频率高低排列
--url		视频流地址，类型：str，默认：http://218.108.71.14:8091/111/stream
--show_video	是否展示实时画面，类型：int，默认：1，范围：0或1
--draw_line	是否手动画线，类型：int，默认：1
--indir		draw_line 1 时有效，设定 in 的计数方向，类型：str，默认：d
		u:up, d:down, r:right, l:left
--outdir	draw_line 1 时有效，设定 out 的计数方向，类型：str，默认：u
--usb_cam	是否使用usb摄像头，类型：action
		使用usb摄像头时，添加该参数，url地址类型变为 int
--cameraId	mqtt消息里摄像头Id，类型：str，默认：00000011
--pre_class	统计类别，类型：int+，默认：[1,2,3,5,7]
		输入格式为：--pre_class 1 2 3 5 7
		共80类，详见README
		常用类别：0 人，1 自行车，2 汽车，3 摩托车，5 大巴车，7 卡车
--width		视频resize宽度，类型：int，默认：640
		对检测准确率基本不造成影响的最小值：256
--height	视频resize高度，类型：int，默认：512
		对检测准确率基本不造成影响的最小值：256
--interval	关键帧间隔，类型：int，默认：5
		每处理N帧，进行一次目标检测
		目标运动速度过快时，或目标运动过程形状、大小变化过大时，减小该参数，可以提高id匹配准确度
--frame_hop	取流跳帧数，类型：int，默认：2
		取流时，隔N-1帧取一帧，最小值为1
		目标运动速度慢时，可以适当增大该参数，速度过快时,减小该参数
		对于车辆跟踪，原帧数间隔一般为10帧合适，即interval乘以frame_hop约等于10
--mqtt		是否发送mqtt消息，类型：int，默认：1，范围：0或1
--conf_thres	检测置信度阈值，类型：float，默认：0.8，范围：0-1
		对于目标较小，或视频像素较低时，可以减小该参数，提高检测概率
		对于目标不够明显或不够典型时，例如人打伞、戴帽子等，可以减小该参数，提高检测率
		对于区分目标类别较严格时，例如更好区别大巴车和卡车时，可以增大该参数
--nms_thres	检测非极大值抑制阈值，类型：float，默认：0.3，范围：0-1
		目标过于密集且产生部分遮挡时，减小该参数，可以减少重叠的边框被抑制掉，提高多目标检测率
		目标没有任何遮挡可以设置为0.5
--iou_thresh	关键帧处跟踪框id匹配阈值，类型：float，默认：0.3，范围：0-1
		不建议调整该参数
--model_def	yolo模型
		可选：yolo和yolo-tiny，模型性能详见README
--weights_path	yolo权重文件
		和yolo模型对应
--empty_count	已计数id列表清零，类型：int，默认：10000
		N帧后，清空已计数id列表，没太大用的参数
--multiprocess	是否多进程，类型：int，默认：1，范围：0或1
--gpu		是否gpu，类型：int，默认：1，范围：0或1
--flow_area	光流跟踪有效面积，类型：float，默认：0.75，范围：0.5-1
		光流值的取值范围，越大跟踪越准确
		理想情况：规则的方形物体，值可以设置为1
		常见情况：大巴车：0.9，卡车：未知，小汽车：0.8，人正面：0.8，人侧面：0.75
		不支持分类别设置，全局统一
--max_err	关键帧匹配的边框偏移最大误差，类型：int，默认：100
		惯性估计关键帧边框位置与实际的最大误差，作为id匹配的一个限制条件
		默认100像素点的距离可视为基本无限制
--center_position_x	中心点位置，类型：float，默认：0.5，范围：0-1
			目标跨计数线的实际计算点，默认几何中心
			坐标原点：左上角
--center_position_y	中心点位置，类型：float，默认：0.5，范围：0-1
			目标跨计数线的实际计算点，默认几何中心

注: * 可以为每一类分别设定不同计数方向和画线位置，须在代码中修改，draw_line须设置0，show_video可为0或1
    * 程序运行时自动创建对应摄像头Id的日志文件夹，保存在./count_log目录下，每个整点记录一次，每天零点计数结果清零，重新开始累计
